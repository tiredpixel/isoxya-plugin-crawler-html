version: "2.4"
services:
  plugin_crawler_html:
    env_file: .env
    build: .
    volumes:
      - ./dist:/home/x/src/dist
      - ./isx-plugin-crawler-html.cabal:/home/x/src/isx-plugin-crawler-html.cabal
      - ./lib:/home/x/src/lib
      - ./src:/home/x/src/src
      - ./test:/home/x/src/test
    networks:
      plugin:
        aliases:
          - crawler-html.plugins.isoxya-stag.pavouk.tech
networks:
  plugin:
    external: true
    name: isx_plugin
