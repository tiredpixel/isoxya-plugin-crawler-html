version: '2.4'
services:
  plugin-crawler-html:
    image: docker.io/isoxya/isoxya-plugin-crawler-html:unstable
    init: true
    networks:
      processor:
        aliases:
          - isoxya-plugin-crawler-html.localhost
    restart: on-failure
networks:
  processor: {}
